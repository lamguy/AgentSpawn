{
  "name": "agentspawn",
  "version": "0.1.0",
  "type": "module",
  "license": "GPL-3.0-only",
  "bin": {
    "agentspawn": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts",
    "dev": "tsup src/index.ts --format esm --watch",
    "test": "vitest run",
    "lint": "eslint src/",
    "format": "prettier --write src/",
    "format:check": "prettier --check src/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "tsup": "^8.4.0",
    "vitest": "^3.0.4",
    "eslint": "^9.19.0",
    "@eslint/js": "^9.19.0",
    "typescript-eslint": "^8.22.0",
    "prettier": "^3.4.2",
    "@types/node": "^22.12.2"
  },
  "engines": {
    "node": ">=20"
  }
}
